# Dependencies
node_modules/

# Build output
dist/
build/

# Environment / secrets
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json

# Uploaded prescription images (app-private)
server/uploads/

# Firebase service account key (never commit)
server/firebase-service-account.json

# Android release keystore (never commit)
client/pharmate-release.keystore

# Internal planning / manuscript companion docs (not for the repo).
# The derived, reproducible seed data (seeds/*.json) IS committed; these raw
# source inputs stay local like the development plan.
PharMate_Development_Plan.md
PharMate_Schedule_Engine_Design.md
Drug Reference.xlsx

# GUI design mockups (reference only; large binaries)
GUI_Draft.pdf
GUI_Draft/

# Local editor/harness tooling
.claude/

# Capacitor generated
client/android/
client/.capacitor/
