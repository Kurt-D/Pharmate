-- Dev seed placeholder.
-- Run via: npm run seed:dev  (executes server/src/db/seed-dev.js)
-- This file is kept as a reference / manual fallback only.
-- The JS seeder is the authoritative dev seed path.
SELECT 'Use `npm run seed:dev` to seed the dev database.' AS instruction;
